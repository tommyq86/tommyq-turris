# tommyq.cz - SMARTHOME configuration
# Proxy /smarthome na lokální Python službu (Google Home webhook)

$HTTP["url"] =~ "^/smarthome" {
    proxy.server = ( "" => ( (
        "host" => "127.0.0.1",
        "port" => 3443
    ) ) )
    proxy.header = ( "https-remap" => "enable" )
}
