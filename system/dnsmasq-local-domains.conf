# Local DNS overrides for *.tommyq.cz
# All subdomains resolve to Turris (192.168.2.1) which reverse-proxies via lighttpd
address=/tommyq.cz/192.168.2.1

# Block AAAA to prevent IPv6 bypass to Cloudflare
address=/tommyq.cz/::

# Block HTTPS/SVCB records (prevents Chrome ECH errors)
filter-rr=HTTPS
